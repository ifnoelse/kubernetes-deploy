
#- name: 检查执行文件路径
#  shell: "for i in {{bin_files|join(' ')}};do cp `find {{tmp_dir}} -name $i` ~/bin;done"
#  register: res

- name: Include variables
  include_vars: "test_vars.yaml"

- include_tasks: "test_task.yaml"

- name: show msg
  debug: msg="hello {{p}}"

#- name: 复制文件测试
#  become: true
#  copy:
#    src: "{{res_dir}}/cfssl_linux-amd64"
#    dest: "{{bin_dir}}/cfssl"
#    owner: root
#    group: root
#    mode: 0755

#- name: 复制文件测试
#  copy:
#    src: ca-config.json
#    dest: "~"