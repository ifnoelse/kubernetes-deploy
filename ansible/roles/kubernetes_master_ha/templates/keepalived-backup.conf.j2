global_defs {
    router_id lb-backup
}

vrrp_instance VI-kube-master {
    state BACKUP
    priority 110
    dont_track_primary
    interface {{ iface }}
    virtual_router_id 51
    advert_int 3
    virtual_ipaddress {
        {{ master_vip }}
    }
}
